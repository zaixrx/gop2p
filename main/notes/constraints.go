/*
defintions:

StateMachine
StateJob
JobStout: { state && output }

StateJobs must be independent in logic(the only thing that connects them is stout)
StateJobs can be transitioned with an External or Internal Actions

External Actions: are actions to be prompted to by the user
Internal Actions: are actions to be prompted to programatically(e.g network request...)

*/
